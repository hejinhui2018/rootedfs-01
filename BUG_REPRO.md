# Bug 复现说明

## 固定基线

正式 Bug 基线以 `main` 分支提交为准，模型修复分支从该提交创建。模型不得修改测试文件。

## 现象

`util.TempDir` 和 `util.TempFile` 在传入空目录时，需要根据文件系统是否处于 chroot 根目录来选择默认位置。非根文件系统不能把宿主机的临时目录直接作为返回路径，否则调用方拿到的路径不再是该虚拟文件系统中的相对路径。

## 专项命令

```bash
go test ./util -run '^TestTempDefaultsRespectChroot$' -count=1 -v
```

基线必须为红灯，修复后必须为绿灯。测试同时覆盖非根文件系统的临时目录、临时文件读回，以及根文件系统的兼容行为。

## 约束

- 只修改必要的生产代码。
- 不修改任何测试文件。
- 保留根文件系统默认临时目录行为。
- 保证返回的临时路径能通过同一个 chroot 文件系统访问。
